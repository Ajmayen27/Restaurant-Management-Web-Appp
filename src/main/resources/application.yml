server:
  port: 8080

spring:
  application:
   name: My-Restaurant

  datasource:

    url: jdbc:postgresql://localhost:5432/My-Restaurant
    username: postgres
    password: 16260

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect